# セッション引き継ぎ

更新: 2026-08-15 ／ 次にやること: **ミラーの解説が古いフレームを返すバグの切り分け**

**これはある時点のスナップショットである。** 恒久的な正本は
[README.md](README.md)・[docs/architecture.md](docs/architecture.md)・
[docs/roadmap.md](docs/roadmap.md) で、内容が食い違ったらそちらが正しい。

---

## いまどこまで出来ているか

| | 状態 |
|---|---|
| `server/` `/api/analyze`（SSE・構造化出力・パック注入） | 動作確認済み |
| iOSアプリ カメラモード（撮影→解析→枠と解説） | **実機で動作確認済み。ハイライトのズレは解決済み** |
| `mac/` Broadcaster（キャプチャ・圧縮・MPC送信） | 実装済み。**M4の測定は全項目合格** |
| iOSアプリ ミラーモード（受信・デコード・表示） | 実装済み。**映像は出る。文字も読める** |
| ミラーでタップして解説 | 実装済み。**バグあり（下記）** |
| 課金・アカウント | 未着手（ベータトークンのみ） |

モデルは OpenAI `gpt-5.6-luna`（Responses API）。**モデルを知っているのは
[server/lib/vision-model.ts](server/lib/vision-model.ts) 1ファイルだけ。**

---

## 🔴 最優先: ミラーの解説が古いフレームを返す

**症状（ユーザー報告）**: ミラー映像の中の別々のボタン（登録ボタン、新規メッセージ
ボタン、誰かのアイコン）をタップしても、**すべて最初にタップしたときの「別の画面」の
解説**が返る。**画面外タップの話ではない。** 映像の中を正しくタップしている。

**意味**: 解析に送られている画像が古い。原因の候補は2つで、**対処が正反対**。

| # | 候補 | 対処 |
|---|---|---|
| A | `displayedPixelBuffer()` が固まったフレームを返している | フレーム取得経路を変える（自前の `VTDecompressionSession` など） |
| B | `displayedPixelBuffer()` が `nil` を返し、リクエストが飛んでいない。前回の解説が画面に残っているだけ | nilになる条件を潰す |

**Bの可能性は実在する。** ヘッダに `This will return NULL if the rate is non-zero`
とあり、レンダラのtimebaseのrateが0でないとnilを返す。本アプリはtimebaseを設定せず
`kCMSampleAttachmentKey_DisplayImmediately` で描画しているので、rateが何になるかは
未確認。

### 切り分け方（実装済み・未検証）

**[MirrorView.swift](ios/UniversalIOCopilot/Views/MirrorView.swift) に計測を入れてある。**
解説パネルに以下が出る:

- **解析した画像そのもののサムネイル**（`session.capturedImage`）
- **解析番号 `#N`**（タップのたびに増える）
- フレームが取れなかった場合 **「Could not read the current frame — nothing was sent」**

これで一目で分かる:

- サムネイルが古い画面 → **候補A**
- `#N` が増えない／「Could not read」が出る → **候補B**

**このビルドはまだ実機に入れていない。** 次のセッションはインストールから。

### 🔴 このバグで私は一度誤診している

「タップが黒帯（レターボックス）に落ちているから」と診断したが**間違い**だった。
ユーザーは映像の中を正しくタップしていた。**症状の報告を自分に都合よく読み替えた。**
次は計測結果を見てから動くこと。

---

## 🟡 未解決: ミラーがカクつく

**再現する。改善していない。原因未特定。**

分かっていること:
- 破綻（画面がぐちゃぐちゃになって戻らない）は**解決済み**（キーフレームを確実配送に、
  欠落検知で最新キーフレームを要求）
- 伝送単体では**片道22ms・5分間損失0%**（実測6）。伝送そのものは十分速い
- **Mac側に毎秒の計測を入れてある**（未検証）:
  ```
    captured 30   sent 28   held  2   keyframes 1
  ```
  - `held` が大きい → 送信抑制が効きすぎ。`--inflight 8` などで緩めて再測
  - `captured` が小さい → 画面が変化していないだけで、カクつきは正常な見え方
  - どちらでもない → 受信側かデコード側

**`--inflight N` の既定値3は根拠のない当てずっぽう。** 測って決めること。

### 通知が来るとフリーズする（ユーザー報告）

**アプリから他アプリの通知を抑制するAPIは存在しない。** ユーザーが集中モードを使うしかない。

ただし**バナー表示自体でフリーズするのは不自然**で、通知受信に伴う通信がAWDL（MPCの無線）と
競合している可能性の方が高い。**集中モードのオン/オフでカクつきを比較すれば切り分けられる。**
未実施。

---

## 🟢 M4の測定は全項目合格（実測4〜6、詳細は roadmap.md）

**企画最大の未知数だった伝送は、制約ではなかった。** 低fpsプレビューへの退避は不要。

| 合格ライン | 実測 |
|---|---|
| 可読性 | ✅ 「文字は小さいが読める」（ユーザー確認） |
| 遅延 片道300ms以内 | ✅ **片道22ms**（13倍の余裕） |
| 5分間切断なし | ✅ **9000個中0損失** |
| 帯域 | 静止 0.33 Mbps / 動作時 5.84 Mbps |
| キャプチャ | 約30 samples/s、論理解像度1728×1117 |

### 測定から確定した設計要件（実装済み）

1. **MPCの `.unreliable` は何も捨てない。** 送りすぎると遅延が無限に伸びる。
   Broadcaster側で送信を止める必要がある（未受領が一定数を超えたら捨てる）
2. **キーフレームは確実配送。** 復旧の唯一の起点で、かつ最大のパケット（最大277KB）。
   これを失うと壊れたまま戻らない
3. **停止から復帰したら最新へ飛ぶ。** 古いフレームを再生すると画面が巻き戻る
4. **パラメータセットをキーフレームごとに載せる。** 途中参加・復帰が握手なしでできる

---

## 次にやること（順序）

1. **上記バグの切り分け**（ビルド→インストール→サムネイルと`#N`を見る）
2. **カクつきの切り分け**（毎秒の数字＋`--inflight`＋集中モード比較）
3. **M5: AX接地** — 下記

### M5: AXは映像からではなくMac側のOSから取れる

**ユーザーから「映像共有だけなのにAXは取りようがないのでは」と質問があった。正当な疑問。**
答えは、**Macコンパニオンは Mac 上で動くので `AXUIElement` に直接問い合わせられる**。
映像とは別経路でOSから実測値が来る。

```
Mac companion ─┬─ ScreenCaptureKit ──→ 映像
               └─ AXUIElement ──────→ 「ボタン"送信"は画面座標(820,410,96,32)」
```

カメラモードでOCRがやっていることを、**推定ではなく実測で、文字のないアイコンにも**
できる。カメラモードにAXが無いのは、目の前が他人の機器でAPIが届かないから。

**受け口は既にサーバーとiOSに実装済み**（`candidates` / `candidate_id`）。
**別途アクセシビリティのTCC権限が必要。**

---

## 動かし方

```bash
cd server && npm run dev          # 必ず dev。start はリクエストを記録しない
```

デバッグ用に受信画像と返却座標を保存する場合（座標問題の切り分けに必須）:

```bash
UIO_DEBUG_CAPTURE_DIR=/tmp/uio-captures npm run dev
python3 scripts/draw-boxes.py /tmp/uio-captures/<request_id>
```

Macコンパニオン:

```bash
cd mac && swift build
./.build/debug/Broadcaster --mirror --seconds 120 --fps 30   # ミラー
./.build/debug/Broadcaster --link --sweep --bytes 30720      # 伝送の測定
./.build/debug/Broadcaster --seconds 10 --fps 30 --encode    # キャプチャと帯域の測定
```

実機への流し方は [ios/README.md](ios/README.md)。UDIDは `B36F0065-61B7-50D1-AA11-0DAF6026CD7A`。

- APIキーは `server/.env.local`（gitignore済み）。**`.env.example` に書かない**
- `ios/Local.xcconfig`（gitignore済み）に接続先とベータトークン。実機では
  `localhost` ではなく**Macのアドレス**（`ipconfig getifaddr en0`）
- **カメラもミラーもSimulatorで動かない。** 検証は実機必須

---

## 支払い済みの授業料（同じ穴に落ちないために）

### 座標・画像

| 罠 | どこに書いてあるか |
|---|---|
| モデルに画像サイズを伝えないと**Y座標だけ**壊れる（解説文は正しいまま） | [server/lib/prompt.ts](server/lib/prompt.ts)、lessons §3-b |
| 伝えた寸法が**格納時**だと、モデルは**回転後**を見ているので同じ壊れ方をする | [server/lib/image-size.ts](server/lib/image-size.ts)、lessons §3-c |
| **写真は実寸で送ると座標が壊れる**（命中3/11）。「縮小すると読み違える」は逆だった | [ios/.../AnalysisSession.swift](ios/UniversalIOCopilot/ViewModels/AnalysisSession.swift)、lessons §3-d |
| 誤差は**バイモーダル**。平均で見て定数補正すると当たっている回を壊す | investigation §4 |
| **座標はモデルに目測させない。** OCR/AXの実測矩形を `candidates` で渡す | lessons §4 |

### 環境・権限

| 罠 | 対処 |
|---|---|
| **画面収録のTCCはCLIではなくターミナルに紐づく** | ターミナルを許可すれば再ビルドしても壊れない。エージェントのシェルからは許可ダイアログを出せない |
| **ポート3000を別プロジェクトが握る**とHTMLが返り、解析だけ失敗する | `curl -X POST .../api/analyze -d '{}'` が401なら自分のサーバー |
| iOSは `NSBonjourServices` が無いと**黙って**MPCを探索しない | `ios/project.yml` に `_uio-mirror._tcp` / `._udp` |
| `NSLocalNetworkUsageDescription` が無いとiOSは**オフラインを装って**失敗する | `ios/project.yml` |
| 実機インストールは**ロック解除が必要**なことがある | `devicectl` が `unavailable` ならロック解除を依頼 |
| SourceKitがプロジェクト内の型を「見つからない」と出すのは**索引のノイズ** | `xcodebuild` / `swift build` の結果だけを信じる |

**共通する形がある。** どれも例外を投げず、**正解の顔をして間違える**か、
**無関係な場所を疑わせるエラーを出す**。

---

## 🔴 進め方についての教訓（このセッションで最も高くついた部分）

**M4だけで計測器の欠陥を4つ踏んだ。うち2回は失敗をもっともらしい数字として出力した。**

| 出力 | 実際 |
|---|---|
| 「損失100%・遅延0ms」 | iPhoneがスリープして応答できなかっただけ |
| 「損失91%」 | 27秒で切断していた。送信先を再確認していなかった |
| 「8.6 fps」 | 同期圧縮がキャプチャを詰まらせていた |
| 「1728x1117 pixels」 | ポイント値を2回表示していた |

**もし直さずにいたら「MPCは91%失うから使えない」と結論し、企画の形を変えていた。**
実際には同じ7.37 Mbpsが5分間・損失0%で通る。

**システムより先に計測器を疑うこと。** そして:

1. **仮説を立てる前に、絵を見る手段を作る。** 座標問題は、モデルが受け取った画像に
   モデルが答えた座標を描いた15分で探索空間が半分になった。それまでの推測は3回とも外れた
2. **誤差は平均ではなく分布で見る**
3. **コメントに書かれた未測定の前提を疑う**（「縮小すると読み違える」は逆だった）
4. **ユーザーの症状報告を自分の仮説に合わせて読み替えない**（今回やった）
